// R-1.3 Give a C++ definition of a struct called Pair that consists of two members.
// The first is an integer called first, and the second is a double called second.

struct Pair {
    int first; // first integer
    double second; // second double
};